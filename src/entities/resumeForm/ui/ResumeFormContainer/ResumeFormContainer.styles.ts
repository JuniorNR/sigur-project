import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StyledResumeFormContainer = styled(Box)(() => ({
  width: '1000px',
  backgroundColor: '#222',
  margin: '0 auto',
  marginTop: '150px',
  padding: '25px',
}));