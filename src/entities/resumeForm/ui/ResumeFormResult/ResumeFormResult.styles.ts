import styled from "@emotion/styled";
import { Box } from "@mui/material";


export const StyledResumeFormResult = styled(Box)(() => ({
  maxWidth: '100%',
  width: '100%',
  display: 'flex',
  gap: '5px',
  flexDirection: 'column',
  alignItems: 'center',
  color: '#fff',
}));